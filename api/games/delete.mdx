---
title: "DELETE Game"
api: "DELETE https://api.nerdapi.com/game"
description: "Esse endpoint deleta um jogo a partir do seu id."
---

### Body

<ParamField body="id" type="string">
  ID do jogo que será deletado.
</ParamField>

### Response

<ResponseField name="success" type="boolean">
  Se requisição foi bem sucedida retorna true, caso contrário retorna false.
</ResponseField>

<RequestExample>

```bash Example Request
curl --location --request DELETE 'https://api.mintlify.com/api/user' \
--header 'Content-Type: application/json' \
--header 'Authorization: Token <token>' \
--data-raw '{
		"user_group_id": "example_1"
		"current_token": "abcdef"
}'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true
}
```

</ResponseExample>
